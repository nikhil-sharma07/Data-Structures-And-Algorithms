#include <bits/stdc++.h>
using namespace std;

void func(vector<int> &nums, int n, int a, int b)
{
    int l = a, r = b;
    while (l < r)
    {
        int mid = (l + r) / 2;
        if ()
    }
}

int main()
{
    int n, a, b;
    cin >> n >> a >> b;
    vector<int> v;
    for (int i = 0; i < n; i++)
    {
        int num;
        cin >> num;
        v.push_back(num);
    }

    func(v, n, a, b);

    return 0;
}